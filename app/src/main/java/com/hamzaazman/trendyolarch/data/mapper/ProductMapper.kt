package com.hamzaazman.trendyolarch.data.mapper

import com.hamzaazman.trendyolarch.data.model.Product
import com.hamzaazman.trendyolarch.domain.model.ProductUiModel
import javax.inject.Inject


/*
class ProductMapper @Inject constructor() {
    fun mapFromResponse(response: List<Product>): List<ProductUiModel> {
        return response.map {
            ProductUiModel(
                id = it.id,
                title = it.title,
                thumbnail = it.thumbnail,
                rating = it.rating,
                images = it.images,
                description = it.description,
                price = it.price,
                category = it.category
            )
        }
    }
}

 */
